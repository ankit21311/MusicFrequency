# Environment variables — copy this to .env and fill in your values
# Never commit the actual .env file to git

# MongoDB Atlas connection string
# Get this from: Atlas Dashboard → Connect → Drivers → Copy URI
MONGODB_URI=mongodb+srv://ankitraj_db_user:0FmD5fcTzMNxW0lk@cluster0.dr0cfhb.mongodb.net/?appName=Cluster0

# Server port (Render sets this automatically in production)
PORT=3000

# Admin credentials (server-side validation)
ADMIN_USERNAME=Gauravi
ADMIN_PASSWORD=Gauravi@1234
